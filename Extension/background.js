// chrome.browserAction.onClicked.addListener(function (tab) {
//     // Toggle the extension state when the extension icon is clicked
//     chrome.tabs.sendMessage(tab.id, { action: "toggleExtension" });
//   });
  